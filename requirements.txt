# A capped requirement takes a three-part floor (>=2.2.0,<3, not >=2.2,<3).
# Dependabot can only spot a major update, and skip it, when it reads the
# current version as three parts.
httpx>=0.27
pytest>=8
fastapi>=0.110
uvicorn>=0.30
mcp>=2.0
# Merchant identification. Capped at <2: a major version changes what the
# SDK accepts, so the next one is its own reviewed change.
anthropic>=1.8.0,<2
# The Anthropic SDK's HTTP layer: Pydantic's maintained fork of httpx. It
# arrives with anthropic, and is declared because
# tests/test_anthropic_sdk.py imports it to fake the API.
httpx2>=2.0
# Passkey (WebAuthn) verification for the UI gate (#22): vetted parsing and
# verification of authenticator responses; pulls in cryptography + cbor2.
# Capped at <3: verification semantics are security-load-bearing, so a major
# bump is its own reviewed change.
webauthn>=2.8.0,<3
